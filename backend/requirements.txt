# Web API
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.7
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0
pgvector>=0.3.0

# Auth
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0

# HTTP / URL ingestion
httpx>=0.27.0
beautifulsoup4>=4.12.0

# Google ADK (agent runtime + sessions)
google-genai>=1.0.0
google-adk>=1.0.0
litellm>=1.0.0

# Embeddings (BGE-M3; pulls torch / transformers — install separately if needed for CUDA)
FlagEmbedding>=1.2.0

# Document loading & chunking
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0
langchain-mineru>=0.1.0
Markdown>=3.5.0
unstructured>=0.16.0
